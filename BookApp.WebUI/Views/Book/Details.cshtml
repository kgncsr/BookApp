@model BookGenreModel
@*<div class="card">
        <div class="row">
            <div class="col-md-3">
                <img src="/img/@Model.Book.ImageUrl" class="card-img" />
            </div>
            <div class="col-md-9">
                <div class="card-body">
                    <h3 class="card-title">@Model.Book.Title</h3>
                    <h6 class="card-title">Yazar : @Model.Book.Writer.Name</h6>
                    @foreach (var item in Model.Categories)
                    {
                        <a href="/">@item.Name</a>
                    }
                    <p class="card-text">@Html.Raw(Model.Book.Description)</p>


                </div>
            </div>
        </div>
    </div>*@

<!DOCTYPE html>
<html lang="tr">
<body>
    <section class="banner-bottom">
        <div class="container">
            <div class="row">
                <div class="col-lg-8 left-blog-info-w3layouts-agileits text-left">
                    <div class="blog-grid-top">
                        <div class="b-grid-top">
                            <div class="blog_info_left_grid">
                                <a href="/Blog/BlogReadAll/@*blog.BlogId*@">
                                    <img src="/img/satranc.jpg" class="img-fluid" alt="">
                                </a>
                            </div>
                            <div class="blog-info-middle">
                                <ul>
                                    <li>
                                        <a href="#">
                                            <i class="far fa-calendar-alt"></i>@DateTime.Today
                                        </a>
                                    </li>
                                </ul>
                            </div>
                        </div>

                        <h1>
                            <a href="">@Model.Book.Title </a>
                        </h1>
                        <p>
                            @Model.Book.Description
                        </p>
                        <a asp-action="AllList" asp-controller="Book" class="btn btn-primary list-group">Blog Listesi</a>
                    </div>
                    @*@await Component.InvokeAsync("CommentListByBlog", new { id = @ViewBag.blogId })*@
                    @*@await Html.PartialAsync("~/Views/Comment/PartialAddComment.cshtml")*@
                </div>
                <aside class="col-lg-4 agileits-w3ls-right-blog-con text-right">
                    <div class="right-blog-info text-left">

                        @await Component.InvokeAsync("BookWriter", new { id = Model.Book.WriterId })
                        <div class="tech-btm">
                            <h3>Kategoriler</h3>
                            <br />
                            <ul class="list-group single">
                                @foreach (var item in Model.Categories)
                                {
                                    <li class="list-group-item d-flex justify-content-between align-items-center">
                                        @item.Name
                                    </li>
                                }

                            </ul>
                        </div>
                    </div>
                </aside>
            </div>
        </div>
    </section>
</body>
</html>

